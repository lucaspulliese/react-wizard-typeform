import { PageWrapper } from './styled';
import FormBuilder from 'app/form-builder';

const Home = () => {
  return (
    <PageWrapper>
      <FormBuilder />
    </PageWrapper>
  );
}

export default Home;
